import React from "react";
import "../styles.css";

const Add = ({}) => {
  return (
    <div className="add">
      <p>Add task</p>
      <button className="add-btn">+</button>
    </div>
  );
};

export default Add;

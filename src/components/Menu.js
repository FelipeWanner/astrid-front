import React from "react";
import "../styles.css";

const Menu = ({}) => {
  return (
    <div className="menu-sec">
      <button className="menu">Today</button>
      <button className="menu">Week</button>
      <button className="menu">Forecast</button>
    </div>
  );
};

export default Menu;
